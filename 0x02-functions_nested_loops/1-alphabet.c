#include "main.h"

/**
 * main - check the code
 * Decription: prints the alphabet, in lowercase
 *
 * Return: Always 0.
 */
int main(void)
{
	char ch
    print_alphabet("alphabet in lowercase: \n");
    for (ch = 97; ch <=122; ch++)
	    _putchar(ch);
    _putchar('\n');
    return (0);
}
